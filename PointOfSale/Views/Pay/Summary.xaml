<UserControl x:Class="Shop.PointOfSale.Views.Pay.Summary"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:b="clr-namespace:Shop.PointOfSale.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <!--<i:Interaction.Behaviors>
            <b:ElementDeferredVisibilityBehaviour VisibileDelay="250" AnimationDuration="250" />
        </i:Interaction.Behaviors>-->
        
        <StackPanel Margin="25" VerticalAlignment="Bottom">
            <TextBlock Text="TOTAL" FontSize="20" FontWeight="Light" Foreground="{StaticResource LabelColorBrush}" TextAlignment="Right" Margin="0,10,0,0" />
            <TextBlock x:Name="TotalText" Text="$9.99" FontSize="30" FontWeight="Light" Foreground="{StaticResource NormalColorBrush}" TextAlignment="Right" />
        </StackPanel>
    </Grid>
</UserControl>
