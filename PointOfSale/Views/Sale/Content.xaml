<UserControl x:Class="Shop.PointOfSale.Views.Sale.Content"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:c="http://www.caliburnproject.org"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:b="clr-namespace:Shop.PointOfSale.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600">
    <Grid>
        <ItemsControl x:Name="Products" Style="{StaticResource PointOfSaleContent}" Visibility="{Binding ContentVisibility}">
            <i:Interaction.Behaviors>
                <b:ElementDeferredVisibilityBehaviour VisibileDelay="100" AnimationDuration="400" />
            </i:Interaction.Behaviors>
        </ItemsControl>                 
    </Grid>
</UserControl>
